import 'package:flutter/material.dart';

class MusicControlPage extends StatelessWidget {
  const MusicControlPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
