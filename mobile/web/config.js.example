// Duplicate this file, rename it to "config.js" in the same directory, 
// and add your Google Maps API key below.
// Note: config.js is git-ignored and safe from being committed.

window.GOOGLE_MAPS_API_KEY = "YOUR_GOOGLE_MAPS_API_KEY_HERE";
