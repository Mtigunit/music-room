import 'package:flutter/widgets.dart';

class MusicControlView extends StatelessWidget {
  const MusicControlView({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
